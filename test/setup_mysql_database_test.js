/**
 * Test case for setupMysqlDatabase.
 * Runs with mocha.
 */
'use strict'

const setupMysqlDatabase = require('../lib/helpers/setup_mysql_database.js')
const assert = require('assert')
const co = require('co')

describe('setup-mysql-database', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Setup mysql database', () => co(function * () {

  }))
})

/* global describe, before, after, it */
